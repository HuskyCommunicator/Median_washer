# 词缀匹配配置文件
# ============================================================================
# 这里使用列表格式 (JSON结构)，以便在高级编辑器中直接查看和修改。
# 结构说明:
# [
#   { "type": "AND", "affixes": ["A", "B"] },            # 必须包含 A 和 B
#   { "type": "COUNT", "min": 2, "affixes": ["C","D","E"] }, # C,D,E 中至少出现 2 个
#   { "type": "NOT", "affixes": ["F"] }                  # 不能包含 F
# ]
# 顶级列表中的所有组之间是 AND 关系（必须同时满足所有组的要求）。

DEFAULT_CONFIGS = {
    # 示例：雪崩杖 (精确匹配)
    # 要求：必须同时拥有 雪崩、冰冻系法术、敌人冰冻
    "默认_雪崩杖": [
        {
            "type": "AND",
            "affixes": ["雪", "冻系", "人冰"]
        }
    ],


    "默认-冰雹项链": [
        { "type": "AND", "affixes": ["冰雹"] },
        { "type": "COUNT", "min": 1, "affixes": ["所有技能", "冰冻系", "人冰"] }
    ],

    "默认-技能法术伤-项链戒指": [
        {
            "type": "AND",
            "affixes": ["所有技能"]
        },
        {
            "type": "COUNT",
            "min": 1,
            "affixes": [
                # 对应元素的法术 + 对应抗性
                "冻系 && 人冰",
                "焰系 && 人火",
                "电系 && 人闪"
            ]
        }
    ]
}

